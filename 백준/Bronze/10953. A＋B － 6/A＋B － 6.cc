#include <bits/stdc++.h>
using namespace std;

int main() {
    // cin.tie(0)->sync_with_stdio(0);
    int t;
    scanf("%d", &t);
    while (t--) {
        int a, b;
        scanf("%d,%d", &a, &b);
        printf("%d\n", a+b);
    }
}