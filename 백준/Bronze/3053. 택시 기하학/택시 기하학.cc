#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(false);
    cout << fixed << setprecision(6);
    long double r;
    cin >> r;
    cout << M_PI*r*r << '\n' << 2*r*r << '\n';
}